import argparse
import numpy as np
import matplotlib.pyplot as plt
import gym
import sys

sys.path.append("../paper_experiments")
import f110_gym

def main():
    parser = argparse.ArgumentParser()
    parser.add_argument('--model', type=str, required=True)
    args = parser.parse_args()
    
    # Read the log generated by the HIL script. Oh wait, HIL script doesn't log X, Y!
    # I will modify run_f110_vegas_hil.py to save trajectory when --render is passed!
